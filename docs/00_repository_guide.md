# Repository guide

The repository is organized around the computational workflow used in the study. Experiment settings are under `configs/experiments/`, model code is under `src/sage_mil/`, and the training, evaluation, retrospective feature-replacement, and robustness utilities are kept in separate directories.

Raw WSI data, third-party pretrained weights, experiment checkpoints, and prediction files are not included.
